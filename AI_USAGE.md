## 09/01/2026 ##
- Question: " Do I need to have the "::eigensoft" at the end of bioconda?" I asked Claude what kind of `bioconda` package that I would need instead of `samtools` for my project idea. My project goal is to work with AADR data which uses `eigensoft` to work with and analyze the data.
- Answer: Claude said yes because AADR uses eigensoft to analyze the data and explained I cannot just include bioconda without a package specification because it is a channel that manages packages.
- Verified: I at first tried to set up the conda environment without specifying a package and it failed to create. 

- Question: " CMD ["conda", "run", "--no-capture-output", "-n", "aadr-project", "python", "src/"] I do not have a script yet, can I just input it like this?" I also asked Claude when setting up my Dockerfile how I should copy the scripts into the environment because the week 2 information had the example of `analyze.py` where I would be using my own scripts for the project.
- Answer: Claude suggested creating a placeholder script `placeholder.py` as well as `placeholder.R` to test the environments and to create the Dockerfile so there was not an issue with directing to an empty folder. (Both of these are located in src\). 
- Verified: Before running anything, I noticed that there was the `analyze.py` script for the week 2 practical, but I had not yet made a script, so I thought it might create an issue.  

- Issue: After setting up renv and running `renv::snapshot()`, the lockfile only recorded the `renv` package itself, not `dplyr`, even though `install.packages("dplyr")` succeeded. Running `renv::restore()` afterward failed with `there is not package called 'dplyr'`. I copy and pasted this error directly into Claude asking why this was happening repeatedly until it was resolved.
- Answer: Claude identified two issues which were 1) several earlier `renv::snapshot()` calls had run before the project was actually activated (I kept getting an "activate project?" prompt I had not understood), so the installs went into my global R library instead of the project (local) one and 2) even after properly activating the project, renv's default "implicit" snapshot mode only records packages that are referenced via `library()`/`require()` in an actual script files in the project. Since I had not written a script that used dplyr yet, renv did not consider it a real project dependency.
- Verified: After multiple failed attempts, I created `.Rproj` so the project reliably activates on open (instead of relying on `setwd()`), which does not trigger `.Rprofile`, added `src/placeholder.R` that calls `library(dplyr)`, and re-ran `renv::snapshot()`. I confirmed dplyr appeared in `renv.lock` by inspecting the file directly with `readLines()`, then verified the entire environment was reproducible by deleting `renv/library` and running `renv::restore()` followed by `library(dplyr)` to confirm.
